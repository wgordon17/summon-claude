# Slack App Manifest for summon-claude
#
# Use this manifest to create the Slack app:
# 1. Go to https://api.slack.com/apps
# 2. Click "Create New App" → "From an app manifest"
# 3. Select your workspace
# 4. Paste the entire contents of this file
# 5. Click "Create"
#
# Then install the app and run `summon init` to configure tokens
#

display_information:
  name: summon-claude
  description: Bridge Claude Code sessions to Slack
  background_color: "#000000"

features:
  bot_user:
    display_name: summon-claude
    always_online: false
  slash_commands:
    - command: /summon
      description: Start a new Claude Code session
      usage_hint: "[auth-code]"
      should_escape: false

oauth_config:
  scopes:
    bot:
      - chat:write
      - channels:manage
      - channels:history
      - channels:read
      - groups:write
      - groups:history
      - groups:read
      - files:write
      - reactions:write
      - users:read
      - commands

settings:
  org_deploy_enabled: false
  socket_mode_enabled: true
  event_subscriptions:
    bot_events:
      - message.channels
      - message.groups
  interactivity:
    is_enabled: true
